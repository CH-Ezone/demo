apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "systan-d700"
    labels:
        app: "systan-d700"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: default-systan-d700.10cb527aed964be2b91d.centralindia.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "systan-d700"
                servicePort: 80