apiVersion: v1
kind: Service
metadata:
    name: "systan-3057"
    labels:
        app: "systan-3057"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "systan-3057"