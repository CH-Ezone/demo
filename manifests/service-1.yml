apiVersion: v1
kind: Service
metadata:
    name: "systan-7bea"
    labels:
        app: "systan-7bea"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "systan-7bea"